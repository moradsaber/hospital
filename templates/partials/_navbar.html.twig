<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('hospital_index') }}"><img src="{{ asset('assets/images/logo.png') }}" alt="Hospital"
        style="width: 40px;height: 30px;"
        ></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('hospital_index') }}">Hospital
                    <span class="sr-only">(current)</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('secteur_index') }}">Secteur</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('service_index') }}">Service</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('room_index') }}">Room</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('bed_index') }}">bed</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('reservation_index') }}">Reservation</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('user_index') }}">Users</a>
            </li>

        </ul>
        <ul class="navbar-nav float-right">
            {% if app.user %}
                <li class="nav-item float-right">
                    <a class="nav-link" href="{{ path('app_logout') }}">LogOut</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

<style>
    .nav-link{
        color: #ffffff !important;
        font-weight: bold;
    }
</style>